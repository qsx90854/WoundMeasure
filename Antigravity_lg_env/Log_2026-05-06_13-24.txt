時間點: 2026-05-06 13:24

對話摘要:
將高亮連線功能擴展到一般模式（非手動模式）。現在只要之前做過梯度 SIFT 量測，不需開啟手動模式，直接在右圖點擊即可高亮最近的匹配連線。

修改檔案: depth_measure_multi_aruco_sbs_camera.py

檔案變更統計:
- 修改: `on_release` 函式的 else 分支（非手動模式），新增對右圖點擊的處理。
- 新增: 若 `view_state['grad_data']` 存在，右圖點擊會自動開啟連線顯示，並高亮最近的匹配連線（紅色）。

詳細變更記錄已保存在 Antigravity_lg_env 資料夾中。
