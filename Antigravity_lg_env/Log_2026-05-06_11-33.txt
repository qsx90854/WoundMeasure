時間點: 2026-05-06 11:33

對話摘要:
新增在手動模式 + 梯度 SIFT 連線顯示中，點擊右圖最近匹配點使對應連線高亮變紅的功能。

修改檔案: depth_measure_multi_aruco_sbs_camera.py

檔案變更統計:
- 修改: `view_state` 新增 `highlighted_grad_line` 欄位追蹤目前高亮的連線索引。
- 修改: `update_display` 中，`grad_lines` 的元素改為 tuple `(con, ptsB[i])`，以便後續能查詢每條連線對應的右圖座標。
- 修改: `on_grad_toggle` 更新為相容 tuple 格式的 grad_lines。
- 修改: `on_release` 的手動模式右圖點擊邏輯拆分為兩種情況：
  (1) 若 `manual_pt_A` 尚未設定：點擊右圖會搜尋最近的匹配點，把最近那條連線設為紅色加粗，其餘恢復青色。
  (2) 若 `manual_pt_A` 已設定：點擊右圖則為確認手動匹配點，觸發 `do_measure`。

詳細變更記錄已保存在 Antigravity_lg_env 資料夾中。
