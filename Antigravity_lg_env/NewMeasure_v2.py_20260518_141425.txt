NewMeasure_v2.py 修改紀錄：
[刪除/修改] 行數: 約 261-270
移除了 `w_alg` 計算與 `live_img_bgr = frame[:, :w_alg].copy()` 的切半邏輯。現在將直接使用完整的相機 `frame` 進行後續運算與顯示。

[刪除/修改] 行數: 約 451
在即時更新的主迴圈中，同樣移除了 `frame[:, :w_alg]`，改為直接複製全畫面 `frame.copy()`。
