檔案：d:\Lightglue\lg_env\depth_measure_multi_aruco_sbs.py
時間：2026-04-23 15:10:20

【UI 增強】新增右圖全局 SIFT 特徵池視覺化
內容：
1. **背景特徵圖層**：在右圖 (ax_B) 新增 `scatter_all_sift_B` 圖層，用於顯示所有預算的 SIFT 特徵點。
2. **條件顯示**：只有在勾選「梯度SIFT匹配」時，這些點才會以灰色、極小點 (s=1) 且半透明 (alpha=0.3) 的形式呈現，像是一層薄霧覆蓋在影像上。
3. **目的**：讓使用者直觀看到右圖特徵點的分佈密度，幫助判斷某個區域是否具備足夠的特徵點供 100 個注入點進行匹配。
原因：回應使用者需求，提供完整的特徵分佈參考。
