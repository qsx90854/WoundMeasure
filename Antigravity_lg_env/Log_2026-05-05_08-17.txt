時間點: 2026-05-05 08:17

對話摘要:
調整 ArUco 除錯存圖邏輯。原邏輯位於 `if res:` 區塊內，若偵測失敗 (res 為 None) 則不會存圖。

修改檔案: depth_measure_multi_aruco_sbs_camera.py

檔案變更統計:
- 修改: 將存圖邏輯移出 `if res:` 區塊。
- 新增: 使用 `cv2.aruco.drawDetectedMarkers` 繪製所有偵測到的標籤，即便左右圖沒有共用標籤也會存出除錯圖，方便判斷失敗原因。

詳細變更記錄已保存在 Antigravity_lg_env 資料夾中。
