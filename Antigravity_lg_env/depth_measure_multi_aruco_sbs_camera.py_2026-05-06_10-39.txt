檔案: depth_measure_multi_aruco_sbs_camera.py
修改時間: 2026-05-06 10:39

[修復的部分]
- 解決了切換不同候選幀時，梯度 SIFT 的「搜索範圍框」與「匹配連線」卡在第一幀無法更新的問題。
- 原本的寫法是在計算時「順便」畫上畫面，這導致切換畫面時沒有重繪機制。現在我將每幀算出來的座標點與框線資料 (`g_ptsA`, `g_ptsB`, `g_rect` 等) 皆存入 `measure_results` 字典中。
- 將畫面的重繪邏輯統一交給 `update_display` 函式。只要您點選了右側的 Frame Radio Button 切換幀數，系統就會讀取該幀專屬的資料並重新繪製對應的框與線。
