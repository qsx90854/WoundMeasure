Measure_SIFT.py 修改紀錄：
[新增] 行數: 1-66
全新建立腳本檔案。功能包含：
- 初始化 cv2.VideoCapture 並套用 1920x1080 @ 30FPS 設定 (相機索引預設為 1)。
- 進入無窮迴圈擷取影像，轉為灰階後交由 `cv2.SIFT_create().detect()` 取出特徵點。
- 透過 `cv2.drawKeypoints` 即時將點畫在畫面上，並在左上角顯示點數目。
- 按 'q' 結束程式。
