檔案：d:\Lightglue\lg_env\depth_measure_multi_aruco_sbs.py
時間：2026-04-23 11:30:15

【偵錯功能】新增 Grad-SIFT 匹配連線與數值輸出
內容：
1. **注入匹配連線**：在開啟「梯度 SIFT 匹配」時，除了顯示點位，現在還會額外繪製青色虛線（ConnectionPatch），將左圖的注入點與右圖的匹配點連接起來，方便確認對位。
2. **數值輸出**：在終端機（Console）同步印出前 10 筆注入點的匹配座標與 SIFT 描述符距離（Distance）。
原因：回應使用者反映「看不到結果」的問題，透過連線與數值文字提供更強的視覺與數據證據。
