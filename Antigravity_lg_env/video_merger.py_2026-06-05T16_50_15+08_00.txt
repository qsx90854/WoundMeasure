修改時間：2026-06-05T16:50:15+08:00
修改檔案：d:\Lightglue\lg_env\video_merger.py

本輪變更為「為 video_merger.py 新增影格數即時讀取與前後相接裁剪範圍設定」。

【減少部分】
第 1 至 279 行：
（原未包含影格範圍輸入框與相關 trace 監聽器的舊版 UI 代碼被覆蓋）。

【新增部分】
第 1 至 490 行：
（包含自動檢測並顯示影片總影格數、Concatenate 模式下開始/結束影格輸入框、即時切換 Radiobutton 時的啟用/禁用邏輯，以及在背景執行緒中定位特定影格 `cap.set(cv2.CAP_PROP_POS_FRAMES, ...)` 並裁剪拼接的全部新代碼）。
