檔案: depth_measure_multi_aruco_sbs_camera.py
修改時間: 2026-05-05 09:19

[新增的部分]
- 在 `get_joint_relative_pose` 中補回共面精煉邏輯。
- 透過 SVD 擬合所有 ArUco 角點的最優平面，並將左圖射線投影至該平面，強制 3D 參考點處於同一物理平面上。

[備註]
這項優化在處理多個 ArUco 標籤時，能有效減少因為個別標籤邊角偵測誤差導致的外參 (RT) 抖動，讓量測結果在不同候選幀之間更加一致。
