檔案修改紀錄
檔案: depth_measure_multi_aruco_sbs_camera.py
時間: 2026-05-08T15-31-07

[減少的部分]
行 632-736:
    刪除了原本在 Live 迴圈中自動計算、更新歷史幀、定時觸發等複雜邏輯。

[新增的部分]
行 632 之後:
    加入手動鎖定模式的狀態變數（locked_L, locked_R, live_L, live_R 等）。
    加入三個 Matplotlib 按鈕：「鎖定左圖」、「鎖定右圖」、「計算深度」。
    在 `on_calc` 回呼函數中整合了原本的位姿解算與深度量測邏輯。
    修改 `while` 迴圈為純粹的顯示與按鈕事件處理，不再自動觸發量測。
