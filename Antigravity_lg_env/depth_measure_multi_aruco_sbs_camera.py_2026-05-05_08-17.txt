檔案: depth_measure_multi_aruco_sbs_camera.py
修改時間: 2026-05-05 08:17

[修正的部分]
- 調整除錯影像存檔位置：由「匹配成功後才存」改為「只要開啟功能就存」。
- 即使 `get_joint_relative_pose` 失敗（例如沒看到標籤或沒有共有 ID），現在依然會把偵測到的內容畫出來並存入 `debug_aruco` 資料夾。
- 這樣使用者可以透過 `debug_L_frame_*.png` 確認參考幀是否有看到標籤。

[備註]
此改動解決了「因偵測失敗而導致完全沒有除錯圖可看」的問題。
