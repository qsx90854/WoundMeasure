對話時間: 2026-06-01T16:34:00+08:00

本次對話與回答摘要：
1. 針對點選後日誌印出「深度為負(在相機後方) | 原始算出Z: -1640.94 mm，且無 rect 也無印原因」的現象進行了幾何與代碼排查。
2. 根本原因：
   - 幾何變換方向算反：代碼在計算從左圖 B (end 尾幀) 到右圖 A (start 首幀) 的相對外參 $R_{rel}, t_{rel}$ 時，實際套用了 A 到 B 的變換公式（$R_B R_A^T$ 代替了 $R_A R_B^T$）。這導致左右圖的視差投影幾何關係被顛倒，使三角化出的三維點被誤算在相機後方（深度 Z 為負值），也讓 Homography 投影在右圖上的預測點 $u_{exp}, v_{exp}$ 超出畫布邊界（因而肉眼看不到 rect）。
   - 無錯誤印出：投影出的預測點超出邊界後，未被 boundary 檢查攔截，因此維持 `visible(True)` 但畫在極大負值坐標處，且無任何 Terminal 提醒。
3. 修正措施：
   - 修正了 `analyze_video_frames` 中三個分支（Average、Best Pair、降級首尾幀）的相對外參 $R_{rel}, t_{rel}$ 計算公式，使其方向與立體幾何精確對齊。
   - 在 `update_display` 中對 $H_{AB}$ 投影出的 $u_{exp}, v_{exp}$ 座標加入 `[0, w]` 和 `[0, h]` 範圍檢查，並在超出範圍時於終端機列印詳細警告資訊。
4. 儲存了變更記錄檔：depth_measure_multi_aruco_sbs_camera_v7_demo_zebra.py_2026-06-01T16_33_00+08_00.txt。
