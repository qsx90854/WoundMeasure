檔案：d:\Lightglue\lg_env\depth_measure_multi_aruco_sbs.py
時間：2026-04-21 17:43:12

【修改】
行數：676
內容：
修正 `matchTemplate` 1D 搜尋的範圍。
修改前：`for t in np.arange(-300, 100, 2):`
修改後：`for t in np.arange(-100, 600, 2):`
原因：右相機的視差是往左偏移（x 變小）。方向向量 `vx` 被強制設為負值（往左）。因此 `t` 必須為正數才能往左尋找正確匹配。舊的 `t` 範圍最高只到 100，完全抓不到 200~300 像素視差的目標。修改後支援最多往左找 600 像素。
