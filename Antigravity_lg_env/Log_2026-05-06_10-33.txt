時間點: 2026-05-06 10:33

對話摘要:
新增「顯示/隱藏梯度 SIFT 連線」按鈕，用於視覺化左圖注入的高梯度特徵點與右圖匹配點之間的連線。

修改檔案: depth_measure_multi_aruco_sbs_camera.py

檔案變更統計:
- 新增: 在 UI 介面底部 (`ax_btn_grad`) 新增 `Button` 元件，標題為「顯示梯度 SIFT 連線」。
- 修改: 在 `do_measure` 函式中，當梯度 SIFT 匹配成功並選出有效配對 (`ptsA_m` 與 `ptsB_m`) 時，會自動建立多條 `ConnectionPatch` 連線。
- 修改: 連線的顯示狀態受新按鈕控制。點擊按鈕可即時切換連線的顯示與隱藏 (`set_visible`)，避免連線過多時畫面雜亂。

詳細變更記錄已保存在 Antigravity_lg_env 資料夾中。
