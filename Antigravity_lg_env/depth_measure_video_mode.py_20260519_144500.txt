檔案: depth_measure_video_mode.py
修改紀錄:
- [修改] 239-242 行: 修改了 `play_loop()` 函式的播放邏輯。原本影片播放到最後一幀時，會強制把時間軸歸零 (`val = 0`)，這會導致光流法 (Optical Flow) 試圖去追蹤「最後一幀跳到第一幀」這種非連續的物理突變，進而把錯誤的追蹤座標與歷史 PnP 矩陣混在了一起，最終算出來的深度當然會是一團亂。現在修改為：播放到底時自動暫停 `app_state['playing'] = False`。這不僅解決了運算被污染的問題，也符合這類分析軟體的直覺操作習慣。
